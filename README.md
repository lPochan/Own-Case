## 🛠️ Pré-requisitos

Para rodar este jogo, você precisa ter o **Python** instalado e a biblioteca **Pygame**.

### Como instalar as dependências:
Abra o seu terminal (no VS Code: `Ctrl + '`) e digite o seguinte comando:

```bash
pip install pygame
